var assert = require('assert');
var path = require('path');
var process = require('process');
var api = require('../index');

describe('infra-api-amqp', function() {
	/*
	describe('#test()', function() {
		it('should return 0', function() {
			assert.equal(api.test(), 0);
		});
	});
	*/
});
